- background
    - pad background sprites so they are power of 2
- gameover
    - create gameover screen
    - detect player falling
    - transition
- real levels
    - design menu screen
    - implement the first level
    - implement end screen
- prettier
    - create some accessories for roof tops
    - allow to get inside building ? (add a sprite layer in front like windows or columns)
- funnier
    - create runner phases (you have to go at a minimum speed which then increases)
    - collect points (gold?)
    - superman mode
- other
    - allow to remap keyboard (or at least avoid arrow keys)
- code improvements
    - special components for state ? (like the fsm and few values such as isFacingRight)
    - snippet to create components