- finishing the grapple
    - create grapple state for player ? (or find a way to avoid interacting with ladders and tyrol while being grappled)
    - change unhooked grapple sprite, looks too complicated
- background
    - check best way to handle multi layer parallax (and tile it somehow)
    - create background resources
    - display background resources (no parallax)
    - add parallax
- gameover
    - create gameover screen
    - detect player falling
    - transition
- real levels
    - desing menu screen
    - implement the first level
    - implement end screen
- prettier
    - create some accessories for roof tops
    - allow to get inside building ? (add a sprite layer in front like windows or columns)
- funnier
    - create runner phases (you have to go at a minimum speed which then increases)
    - collect points (gold?)
- code improvements
    - special components for state ? (like the fsm and few values such as isFacingRight)
    - snippet to create components